from setuptools import find_packages, setup

setup(
    name='arquix',
    packages=find_packages(include=['arquix']),
    version='0.1.0',
    description='Declarative workflow configuration manager for Arch-based Distributions.',
    author='Miguel Neto',
)
